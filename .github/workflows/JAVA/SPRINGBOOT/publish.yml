name: Publish

on:
  workflow_call:
  #  inputs:
  #    username:
  #      required: true
  #      type: string
  #  secrets:
  #    token:
  #      required: true

jobs:
  Build:
    name: Publish
    runs-on: ubuntu-latest
    steps:
      - run: echo "Running Publish from JAVA/QUARKUS"
        #with:
        #  username: ${{ inputs.username }}
        #  token: ${{ secrets.token }}
