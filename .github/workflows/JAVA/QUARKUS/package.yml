name: Package

on:
  workflow_call:
  #  inputs:
  #    username:
  #      required: true
  #      type: string
  #  secrets:
  #    token:
  #      required: true

jobs:
  Build:
    name: Package
    runs-on: ubuntu-latest
    steps:
      - run: echo "Running Package from JAVA/QUARKUS"
        #with:
        #  username: ${{ inputs.username }}
        #  token: ${{ secrets.token }}
