name: Tests

on:
  workflow_call:
    inputs:
      stack:
        type: string
        required: true
      tests:
        type: string
        required: true
      repo_onwer:
        type: string
        required: true
      repo_name:
        type: string
        required: true
      pat:
        type: string
        required: true
        description: Personal Access Token (PAT)
    #secrets:
    #  my_pat:
    #    required: true

jobs:
  # JAVA SPRINGBOOT STACK

  tests_java_spring:
    runs-on: windows-latest
    if: ${{ inputs.tests == 'true' && inputs.stack == 'JAVA SPRINGBOOT' }}
    steps:
      #Its needed to check out the target repo in every separeted job
      - name: Check out target repo
        uses: actions/checkout@master
        with:
          repository: ${{ inputs.repo_owner }}/${{ inputs.repo_name }}
          token: ${{ inputs.pat }}

      #its possible to use a shell script to run the tests
      - run: |
          echo "Running tests from JAVA SPRINGBOOT"
          ls -la

  # JAVA MAVEN STACK

  tests_java_maven:
    runs-on: windows-latest
    if: ${{ inputs.tests == 'true' && inputs.stack == 'JAVA MAVEN' }}
    steps:
      #Its needed to check out the target repo in every separeted job
      - name: Check out target repo
        uses: actions/checkout@master
        with:
          repository: ${{ inputs.repo_owner }}/${{ inputs.repo_name }}
           token: ${{ inputs.pat }}

      #its possible to use a shell script to run the tests
      - run: |
          echo "Running build from JAVA MAVEN"
          ls -la
